<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:name=".application.MyApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LessonNotes"
        tools:targetApi="31">
        <activity
            android:name=".activity.SubjectInfoActivity"
            android:exported="false"
            android:label="@string/title_activity_subject_info"
            android:theme="@style/Theme.LessonNotes" />
        <activity
            android:name=".activity.CreateSubjectActivity"
            android:exported="false"
            android:label="@string/title_activity_create_subject"
            android:theme="@style/Theme.LessonNotes" />
        <activity
            android:name=".activity.TermInfoActivity"
            android:exported="false"
            android:label="@string/title_activity_term_info"
            android:theme="@style/Theme.LessonNotes" />
        <activity
            android:name=".activity.CreateTermActivity"
            android:exported="false"
            android:label="@string/title_activity_create_term"
            android:theme="@style/Theme.LessonNotes" />
        <activity
            android:name=".activity.SettingsActivity"
            android:exported="false"
            android:label="@string/title_activity_settings"
            android:theme="@style/Theme.LessonNotes" />
        <activity
            android:name=".activity.TermsActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.LessonNotes">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>